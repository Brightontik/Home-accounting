&НаСервере
Процедура ПриЧтенииНаСервере(ТекущийОбъект)
    // Достаем байты из хранилища и превращаем в навигационную ссылку
    Если ЗначениеЗаполнено(Объект.Файл) Тогда
        ПутьККартинке = ПоместитьВоВременноеХранилище(Объект.Файл.Получить());
    КонецЕсли;
КонецПроцедуры